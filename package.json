{
  "name": "hades-run-randomiser-poc",
  "dependencies": {},
  "devDependencies": {
    "@rollup/plugin-json": "^4.1.0",
    "@rollup/plugin-typescript": "^6.0.0",
    "@types/jest": "^26.0.14",
    "jest": "^26.5.3",
    "npm-run-all": "^4.1.5",
    "rollup": "^2.29.0",
    "serve": "^11.3.2",
    "ts-jest": "^26.4.1",
    "tslib": "^2.0.2",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:coverage": "jest --coverage"
  }
}
